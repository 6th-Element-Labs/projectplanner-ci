# Mission Bot v4 clean lifecycle proof R5

This fresh Mission Bot v4-only lifecycle proves that C3 terminal receipt projection requests `review_merge` and that the exact-head review executes instead of yielding.
