# Auth Caddy reverse_proxy fragment (ARCH-MS-84 drill → applied in ARCH-MS-76).
#
# Live production cut lives in deploy/Caddyfile (handle /api/auth* → 127.0.0.1:8121).
# Keep this file as the rollback/re-cut reference snippet. Pair with:
#   docs/runbooks/auth-caddy-cutover-rollback.md
#   deploy/switchboard-auth.service
#
# Rollback = delete the Auth handle blocks from the live Caddyfile and `caddy reload`
# (monolith :8110 still mounts Auth).

# handle /api/auth* {
#     reverse_proxy 127.0.0.1:8121 {
#         transport http {
#             dial_timeout 5s
#             response_header_timeout 90s
#         }
#         health_uri /health
#         health_interval 5s
#         health_timeout 3s
#         fail_duration 10s
#         lb_try_duration 5s
#         lb_try_interval 250ms
#     }
# }
