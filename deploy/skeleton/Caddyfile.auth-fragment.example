# ARCH-MS-84 — commented Caddy reverse_proxy snippet for a future Auth process cut.
# Paste into deploy/Caddyfile ONLY after independence Go (ARCH-MS-75) and a soak on
# a second uvicorn. Until then, leave unused so live `/api/auth*` stays on the
# monolith (:8110). Rollback = delete these handle blocks and `caddy reload`.
#
# Pair with: docs/runbooks/auth-caddy-cutover-rollback.md
# Port: replace 8121 with the Auth unit's localhost port (skeleton default is 8120;
# keep Auth on a distinct port when both are present).

# handle /api/auth/health* {
#     reverse_proxy 127.0.0.1:8121 {
#         transport http {
#             dial_timeout 2s
#             response_header_timeout 5s
#         }
#     }
# }
# handle /api/auth* {
#     reverse_proxy 127.0.0.1:8121 {
#         transport http {
#             dial_timeout 5s
#             response_header_timeout 90s
#         }
#         health_uri /health
#         health_interval 5s
#         health_timeout 3s
#         fail_duration 10s
#     }
# }
